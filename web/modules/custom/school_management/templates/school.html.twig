{#
/**
 * @file
 *
 * Default template for schools.
 *
 * Available variables:
 * - attributes: HTML attributes for the wrapper.
 * - school: The rendered school fields.
 *   Use 'school' to print them all, or print a subset such as
 *   'store.name'. Use the following code to exclude the
 *   printing of a given field:
 *   @code
 *   {{ school|without('name') }}
 *   @endcode
 * - school_entity: The store entity.
 * - school_url: The store URL.
 *
 * @ingroup themeable
 */
#}
<article{{ attributes }}>
  {{ school }}
</article>
